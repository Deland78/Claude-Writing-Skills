name: scene_cards
version: 1.0
description: >
  Convert chapter promise + tension map into structured scene cards with goal,
  obstacle, turn, cost, and exit hook.
inputs:
  required:
    - chapter_promise_output
    - tension_map_output
  optional:
    - character_bible
    - setting_notes
steps:
  - Generate 3-7 scene cards.
  - Each card includes:
      POV, location, time, goal, obstacle, tactic, turn, cost, exit emotion, next hook.
  - Ensure each scene has a TURN that changes the situation.
  - Ensure each scene ends with propulsion (not closure).
output_format:
  type: yaml
  structure:
    - "scenes:"
    - "  - id:"
    - "    pov:"
    - "    location:"
    - "    time:"
    - "    goal:"
    - "    obstacle:"
    - "    tactic:"
    - "    turn:"
    - "    cost:"
    - "    exit_emotion:"
    - "    next_hook:"
quality_checks:
  - "Every scene has a goal and an obstacle."
  - "Every scene has a turn and a cost."
  - "No scene exists only to deliver exposition."
